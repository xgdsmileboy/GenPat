run cluster:8
PATTERN:
+				if (this.resolvedType == null) {
+					return null;
+				}

TP, FP = 0,0
original_before=
@Override public String toString(){
return "@"+_binding.getAnnotationType().debugName();
}
